---
title: DualMask 实验记录模板
status: template
experiment_id: null
sample_batch: null
baseline_ref: null
purpose: null  # 诊断 / 性能优化 / 机制消融
source_manifest: null
code_commit: null
launch_status: unconfirmed
decision: pending
---

# 实验目的

问题、唯一变更、事先的通过/停止标准：

查重：已检索的关键词、已有实验ID、为何不是重复运行（补配对/明确复现/不同问题）。

# 可比性

数据集/协议、seed、机器、GPU、代码提交、配置 JSON、CLI 覆盖项、Task0/后续 epoch、CA、SDPA、训练集划分、原始日志路径：

提交/环境/预训练文件/数据划分无法确认时填null；不要拿当前HEAD或当前JSON填历史实验。注明基线来源ID和同机对应行。

# 运行健康与完成情况

每组完成任务数、异常、是否为短测、训练时长：

更新后运行可附records目录下effective_config.json、run_info.json和result.json。running不是实时进程状态；completed不代表性能有效。

区分planned / implemented / launch_unconfirmed / awaiting_logs / completed / failed。模型训练与外层队列状态分别记。

# 完整性能结果

Average、Last、Task1–末任务阶段平均 Old/New、最终 Old/New、Forgetting、Task0；与同机同 seed 基线的配对差值：

# 诊断和结论

机制实际是否生效；数据支持什么；单 seed 或协议差异带来的限制；停止/继续决定：

# 原始材料

存入 `docs/experiments/raw/experiments/YYYY.MM.DD_描述_EXPID/`，在 `来源.md` 记录来源和 SHA-256。标准化日志加入 `实验日志/DualMask/类型/`，最后更新 `实验索引.md`；仅有脚本或未完成的运行标记为待运行，不能写成性能结果。
