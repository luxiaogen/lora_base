---
title: DualMask 实验异常与可比性记录
updated: 2026-09-26
status: living_log
---

# 异常与纠正

| 编号 | 类型 | 证据/表现 | 处理与仍未知事项 |
| --- | --- | --- | --- |
| A-001 | 启动检查失败 | CA两次短测完成，随后准确率严格相等断言报`(1, post_merge)` | eb92851删除检查；不修改训练。该次正式T10未开始；CA前差异根因未确定。详见004及归档附件 |
| A-002 | 台账过时 | 003/索引曾写CA未实现，并描述checkpoint分叉 | 已回填13b0bf8/eb92851；实际是独立完整训练，不是快照分叉；完整结果未收到 |
| A-003 | 默认配置漂移 | 旧projection实验有CA10；后续显式CA5。冲突正则开关/权重也存在历史差异 | 同机配对；不将所有86/87分结果当同一基线，不用当前JSON反推历史配置 |
| A-004 | 可复现边界 | 已使用math-SDPA，仍不能保证跨GPU/环境或所有完整训练逐位一致 | 两机各设参照；记录Task0，不以测试准确率完全相等作为启动门槛 |
| A-005 | 预算语义 | 自适应conflict_ratio是下限/参考，不是固定选中量；阈值并列可增加选中数 | 005固定数量、固定强度并另报P有效交集；不称模型稀疏率 |
| A-006 | 解析误判 | 通用解析器按Learning on计任务，并默认total_sessions判断短程；空配置值可能跨行读取 | 本次目录改用最终CNN曲线长度、max_tasks与最终摘要；只对本次离线解析修正空白匹配，未改训练代码 |
| A-007 | 来源重复 | 同内容可下载到不同日期目录；外层队列与内层日志可能重复描述运行 | 相同文件SHA去重、保留来源别名；不同SHA不自动算新实验，仍按前缀/时间和配置核对 |
| A-008 | 选择偏差 | 历史Task0最高分、最佳测试覆盖率可能被误用为固定配方依据 | 保留全量结果；超参选择需训练holdout，不挑测试最高checkpoint |
| A-009 | 缺失历史身份 | 许多早期日志未记录代码提交、数据/权重哈希或完整环境 | 记为null/未知；不得填a58029a作为历史训练提交；目录仅用于检索 |

详细入口：[实验索引](实验索引.md)、[查重主线](实验日志/DualMask/研究收敛/DM-B-260926-006.md)。
这里只记录已有证据；训练完成、机制启用、性能获益是三个独立判断。
